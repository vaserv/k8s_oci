# Sample manifests

These manifests show the static HTML site pattern.

- nginx serves the HTML
- an OCI image volume provides the content
- an Ingress exposes the route over HTTPS

Copy the files into a new site and change the names, host, and image reference.
