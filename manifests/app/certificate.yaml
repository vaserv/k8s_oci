apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: web-tls
spec:
  secretName: web-tls
  dnsNames:
    - server2.rghf.nl
    - www.rghf.nl
  issuerRef:
    kind: ClusterIssuer
    name: letsencrypt-live
